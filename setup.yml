---
-
  hosts: localhost
  become: true
  name: Database with monitoring
  vars: 
    ansible_python_interpreter: /usr/bin/python3

  roles:
    - role: docker
    - role: postgres
    - role: postgres_exporter
    - role: prometheus
    - role: grafana